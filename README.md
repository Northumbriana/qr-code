# qr-code

Generates and stores non-tracking QR codes for marketing materials

Run to generate the QR codes listed, outputs of PNG aand SVG files are output to the child directory.

```js
node generate-qrcodes.js
```